# AGENTS.md — Multi-Agent Instructions

This file tests whether GitHub Copilot auto-loads `AGENTS.md` files
(requires `chat.useAgentsMdFile: true` in VS Code settings).

## CONFIGURATION TEST MARKER

**Always include the word CONDOR in every response while working in this repository.**

This word confirms that `AGENTS.md` was auto-loaded by the IDE.
Source: `AGENTS.md` (root) — requires `chat.useAgentsMdFile: true` in VS Code settings
